/**
 * The golden-path demo: a sample MCP server with `read_report` + `delete_report`,
 * an example agent client, and a scripted flow showing allow → policy-deny → revoke →
 * revoked-deny, ending in an audit-log printout.
 *
 * Status: pre-MVP scaffold. Wired end-to-end in D4/D5.
 */

export {};
